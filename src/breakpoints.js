const sizes = {
    sm: '400px',
    md: '700px',
    bg: '1070px'
}

const breakpoints = {
    sm: `(max-width: ${sizes.sm})`,
    md: `(max-width: ${sizes.md})`,
    bg: `(max-width: ${sizes.bg})`
}

export default breakpoints;